<UserControl x:Class="RdpManager.Views.AddRdpConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RdpManager.Views"
             xmlns:CustomControls="clr-namespace:CustomControls;assembly=CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <Button Content="Import RDP file (optinal)"
                Padding="0 10 0 10"
                Margin="0 0 0 10"
                BorderThickness="0"
                Command="{Binding ImportRdpFileCommand}"/>
        <Label Content="Name:"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Address:"/>
        <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Username:"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Password:"/>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Notes:"/>
        <CustomControls:CustomRichTextBox x:Name="PasswordBoxControl"
                                          Text="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button Content="Add Connection"
                Command="{Binding AddRdpConnectionCommand}"
                HorizontalAlignment="Center"
                Margin="20 30 20 0"
                Padding="20 5 20 5"
                BorderThickness="0"/>
    </StackPanel>
</UserControl>
